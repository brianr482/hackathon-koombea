import { ProductFilterOptionPipe } from './product-filter-option.pipe';

describe('ProductFilterOptionPipe', () => {
  it('create an instance', () => {
    const pipe = new ProductFilterOptionPipe();
    expect(pipe).toBeTruthy();
  });
});
